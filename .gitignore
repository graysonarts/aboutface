/target
dist
tmp

# Corpus data must never be committed — it is photographs of people.
/corpus
# Where the keypress example writes its Captures, for the same reason. Not
# anchored: the example writes relative to the working directory, so this must
# match wherever it is run from.
captures/
*.sqlite
*.sqlite-journal
*.sqlite-wal

# Model weights are fetched, not vendored.
/models
*.onnx
